const TOKEN = 'token'
const RES_Ok = 0
const TIME_STAMP = 'timeStamp' // token时间戳
const TOKEN_TIMEOUT_VALUE = 60 * 60 * 2 * 1000 // 超时时长（毫秒），2小时
const LANG = 'language' // 国际化
const MAIN_COLOR = 'mainColor' // 主题色
const DEFAULT_COLOR = '#304156' // 默认色值
const SIDERBAR_MENUS = 'siderbarMenus' // 侧边导航菜单项

export {
  TOKEN,
  RES_Ok,
  TIME_STAMP,
  TOKEN_TIMEOUT_VALUE,
  LANG,
  MAIN_COLOR,
  DEFAULT_COLOR,
  SIDERBAR_MENUS
}
