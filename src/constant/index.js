const TOKEN = 'token'
const RES_Ok = 0
const TIME_STAMP = 'timeStamp' // token时间戳
const TOKEN_TIMEOUT_VALUE = 60 * 60 * 2 * 1000 // 超时时长（毫秒），2小时

export { TOKEN, RES_Ok, TIME_STAMP, TOKEN_TIMEOUT_VALUE }
